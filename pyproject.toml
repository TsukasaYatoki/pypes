[project]
name = "pypes"
version = "0.1.0"
description = "A terminal animation of pipes flowing across the screen"
requires-python = ">=3.9"

[project.scripts]
pypes = "pypes.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
